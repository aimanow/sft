server {
    listen 80;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    ssl_certificate /etc/nginx/ssl/test.sft.space.crt;
    ssl_certificate_key /etc/nginx/ssl/test.sft.space.key;
    ssl_protocols SSLv3 TLSv1;
    ssl_ciphers ALL:!aNULL:!ADH:!eNULL:!LOW:!EXP:RC4+RSA:+HIGH:+MEDIUM;
    server_name test.sft.space www.test.sft.space;

    root /var/www/html/public;

    location / {
        proxy_pass http://frontendapp:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 443 ssl;
    ssl_certificate /etc/nginx/ssl/sft.space.crt;
    ssl_certificate_key /etc/nginx/ssl/sft.space.key;
    ssl_protocols SSLv3 TLSv1;
    ssl_ciphers ALL:!aNULL:!ADH:!eNULL:!LOW:!EXP:RC4+RSA:+HIGH:+MEDIUM;
    server_name sft.space www.sft.space;

    root /var/www/html/public;

    location / {
        proxy_pass http://frontendapp:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 443 ssl;
    server_name localhost www.localhost;

    root /var/www/html/public;

    location / {
        proxy_pass http://frontendapp:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}